---
alwaysApply: true
---

# Next.js 15+ Rules for THOF Project

## NextJS 15+ Best Practices

### App Router Structure

- ✅ Use App Router exclusively (not Pages Router)
- ✅ Follow the `app/` directory structure with proper nesting
- ✅ Use Server Components by default, Client Components only when needed
- ✅ **REQUIRED**: Implement error.tsx, not-found.tsx for all route segments
- ✅ Use route groups `(public)` for organization
- ✅ Use static `export const metadata` for SEO; avoid `generateMetadata`

### Server Components & Client Components

```typescript
// ✅ Server Component (default) - Modern pattern
export default async function ServerComponent() {
  const data = await fetchData()
  return <div>Server-rendered: {data}</div>
}

// ✅ Client Component (explicit) - Only when needed
'use client'
export default function ClientComponent() {
  const [state, setState] = useState()
  return <div>Interactive content</div>
}

// ❌ AVOID: React.FC (deprecated pattern)
const Component: React.FC<Props> = () => {} 

// ✅ USE: Modern function declarations
export function Component(props: Props) {}
```

### Metadata & Params Handling (Project Policy)

```typescript
// ✅ PROJECT RULE: Do NOT use generateMetadata anywhere
// Use a static metadata export instead
export const metadata: Metadata = {
  title: 'Page Title',
  description: 'Page description',
};

// ✅ Correct params typing for pages/components
// Params is a plain object – NEVER Promise-typed
export default async function Page({ params }: { params: { locale: string; id?: string; slug?: string } }) {
  const { locale } = params
  /* ... */
}

// ❌ AVOID: Promise-typed params and generateMetadata
// export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {}
// export default async function Page({ params }: { params: Promise<{ locale: string }> }) {}
```

### Suspense Usage Rules

```typescript
// ✅ CORRECT: Suspense for code splitting
const LazyComponent = lazy(() => import('./Component'))
<Suspense fallback={<Loading />}>
  <LazyComponent />
</Suspense>

// ❌ WRONG: Suspense around client components
<Suspense fallback={<Loading />}>
  <ClientComponentWithOwnLoading /> {/* Already handles loading */}
</Suspense>

// ✅ CORRECT: Client components handle their own loading
export function ClientList() {
  const [loading, setLoading] = useState(true)
  if (loading) return <LoadingSpinner />
  return <div>Content</div>
}
```

### Error Handling & Boundaries

```typescript
// ✅ REQUIRED: error.tsx in each route segment
'use client'
export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center p-4">
      <Card className="max-w-md w-full p-8 text-center">
        <h1 className="text-2xl font-bold mb-4">Bir Hata Oluştu</h1>
        <p className="text-gray-600 mb-6">
          Üzgünüz, beklenmeyen bir hata oluştu. Lütfen tekrar deneyin.
        </p>
        <Button onClick={reset}>Tekrar Dene</Button>
      </Card>
    </div>
  )
}
```

- ✅ **REQUIRED**: Create error.tsx files for each route segment
- ✅ Use proper error boundaries with reset functionality
- ✅ Implement user-friendly error messages
- ✅ Log errors appropriately for debugging

### Data Fetching

- ✅ Use async Server Components for data fetching
- ✅ Implement proper error boundaries
- ✅ Use Suspense ONLY for legitimate streaming/lazy loading
- ✅ Cache data appropriately with `cache()` and `unstable_cache()`
- ✅ Prefer handling dynamic titles/descriptions in-content; keep `metadata` static

### Performance Optimizations

- ✅ Use `next/image` with proper sizing and optimization
- ✅ Define static metadata via `export const metadata` (no `generateMetadata`)
- ✅ Use streaming with Suspense (for appropriate use cases)
- ✅ Optimize bundle size with dynamic imports for code splitting

## 🚀 Next.js 15+ Configuration

### Modern Configuration (Turbopack Stable)

```typescript
// ✅ CORRECT: Next.js 15+ config
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Production optimizations
  output: 'standalone',
  compress: true,
  
  // Image configuration
  images: {
    // Enable optimization when possible
    unoptimized: false, // Only true if deployment requires it
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
  
  // Build quality policy (non-strict builds for now)
  typescript: {
    ignoreBuildErrors: true,
  },
  eslint: {
    ignoreDuringBuilds: true,
  },
  
  experimental: {
    // Keep empty unless using documented, stable experimental flags
  },
}

// ❌ INVALID: Unrecognized keys under unsupported sections will fail
// Do not add undocumented keys (e.g., custom turbopack options)
```

### Turbopack Usage Rules (September 2025)

- ✅ Development
  - Prefer Turbopack for dev: `pnpm dev` already passes `--turbopack`
  - If you hit a Turbopack-specific issue: temporarily use `next dev` without the flag for diagnosis, then revert
- ✅ Production
  - Use `next build` (Turbopack pipeline under the hood in Next.js 15+)
  - Do not add `turbopack` custom keys in `next.config.ts` (unsupported)
- ✅ Caching & Performance
  - Keep `output: 'standalone'` and `compress: true`
  - Rely on default incremental rebuilds; avoid custom Turbopack tuning keys
- 🔧 Troubleshooting
  - Clear caches on odd rebuilds: delete `.next/` and rerun
  - For dependency graph issues, try `bash ./scripts/clean-install.sh`
  - Validate TS/ESLint separately: `pnpm type-check`, `pnpm lint`
  - If a transformation bug is suspected, compare behavior with `next dev` (no `--turbopack`) and file an upstream issue

### Development Commands

```bash
# ✅ Use Turbo by default
pnpm dev              # Uses --turbopack automatically
pnpm build            # Production build with Turbo
pnpm type-check       # TypeScript validation
pnpm lint             # ESLint validation
```

### Component Patterns

```typescript
// ✅ CORRECT: Modern function components
export function MyComponent({ title }: { title: string }) {
  return <h1>{title}</h1>
}

export default function MyPage() {
  return <div>Page content</div>
}

// ❌ AVOID: Deprecated React.FC
const MyComponent: React.FC<Props> = ({ title }) => {
  return <h1>{title}</h1>
}
```

## 🌐 Internationalization

### i18n Best Practices

- ✅ Use next-intl for internationalization
- ✅ Structure components for i18n from the start
- ✅ Externalize all user-facing strings
- ✅ Use semantic keys for translations
- ✅ Consider RTL language support

### i18n Implementation

```typescript
// ✅ CORRECT: Server component with translations
import { useTranslations } from 'next-intl'

export default function ServerPage() {
  const t = useTranslations('page')
  return <h1>{t('title')}</h1>
}

// ✅ CORRECT: Client component with translations
'use client'
import { useTranslations } from 'next-intl'

export function ClientComponent() {
  const t = useTranslations('component')
  return <button>{t('submit')}</button>
}
```

## 📊 Performance & Monitoring

### Core Web Vitals

- ✅ Monitor LCP (Largest Contentful Paint)
- ✅ Track FID (First Input Delay)
- ✅ Measure CLS (Cumulative Layout Shift)
- ✅ Use Next.js built-in analytics

### Performance Optimizations

```typescript
// ✅ CORRECT: Optimized images
import Image from 'next/image'

<Image
  src="/image.jpg"
  alt="Description"
  width={800}
  height={600}
  sizes="(max-width: 768px) 100vw, 50vw"
  priority // For above-the-fold images
/>

// ✅ CORRECT: Dynamic imports for code splitting
import dynamic from 'next/dynamic'

const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
  loading: () => <Loading />,
  ssr: false // If client-only
})
```

## 🚀 Deployment & DevOps

### Environment Management

- ✅ Use .env files for configuration
- ✅ Validate environment variables at startup
- ✅ Never commit secrets to version control
- ✅ Use different configs for dev/staging/prod

### Docker Best Practices

- ✅ Use multi-stage builds
- ✅ Optimize image sizes
- ✅ Follow security best practices
- ✅ Use proper environment variables

## 📝 Code Quality

### TypeScript Best Practices

- ✅ Use strict TypeScript configuration
- ✅ Define proper types for all props
- ✅ Avoid `any` type usage
- ✅ Use proper generic constraints

### Testing Patterns

- ✅ Write unit tests for utilities
- ✅ Integration tests for API routes
- ✅ E2E tests for critical user flows
- ✅ Use proper mocking strategies

---
description: Next.js 15+ best practices and patterns for the THOF project
globs: ["**/*.tsx", "**/*.ts", "next.config.ts", "**/*.mdc"]
alwaysApply: true
---