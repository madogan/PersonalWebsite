# Auth (required for admin panel)
AUTH_SECRET=   # Generate with: openssl rand -base64 32
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Optional; required for admin "Generate with Gemini" blog draft generation
GEMINI_API_KEY=
